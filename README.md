# WindowsInternal.ComposableShell.Experiences.SuggestionUIUndocked

Windissect analysis output for `WindowsInternal.ComposableShell.Experiences.SuggestionUIUndocked`, archived per build so the
`headers/` carries 376 recovered C++ class headers.
`rust/` carries generated Rust bindings (3 source files).

structure of Windows can be diffed across releases.

## Builds

| build | IR (raw) | IR (gzip) |
| --- | ---: | ---: |
| `1785167607-CA43E8E793B54D15A2CF904E62B4B0BE-1` | 90.9 MB | 4.8 MB |

`ir.json` is stored gzipped: a full-depth record for a shell binary runs to
hundreds of megabytes, and GitHub refuses any file over 100 MB.

```sh
gunzip -c history/WindowsInternal.ComposableShell.Experiences.SuggestionUIUndocked/builds/<build>/ir.json.gz > ir.json
```

Diff two builds with:

```sh
windissect diff --old old/ir.json --new new/ir.json --markdown-out changes.md
```

Generated by [windissect](https://github.com/razecrs/windissect).
